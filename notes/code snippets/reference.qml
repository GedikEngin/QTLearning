// default imports
import QtQuick 2.15
import QtQuick.Controls 2.15


// the application window that appears and is connected to the cpp engine
ApplicationWindow {
    visible: true
    // dimensions of the window
    width: 640
    height: 480
    // name of the window
    title: "Hello Qt QML"
    // basic background with color control
    background: Rectangle {
        color: "gray" }
}

